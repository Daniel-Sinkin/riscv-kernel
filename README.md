# riscv-kernel

A small RISC-V kernel project targeting QEMU `virt`, built with GCC and CMake.

## Screenshot

Naive heap allocator layout sketch:

![Naive malloc layout](screenshots/MallocNaive.png)
